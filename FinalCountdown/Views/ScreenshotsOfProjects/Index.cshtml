@model IEnumerable<FinalCountdown.Models.ScreenshotsOfProject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScreenshotsOfProject1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScreenshotsOfProject1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScreenshotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ScreenshotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScreenshotID })
        </td>
    </tr>
}

</table>
